# Model summary callback -- prints model architecture at the start of training.
# All fields correspond to ModelSummaryCallback.__init__ parameters.
# Instantiate with: hydra.utils.instantiate(cfg.callbacks.model_summary)

model_summary:
  _target_: deepfont.callbacks.ModelSummaryCallback
  max_depth: 1
